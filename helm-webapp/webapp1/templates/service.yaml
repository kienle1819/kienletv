apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapp1.fullname" . }}
  labels:
    {{- include "webapp1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webapp1.selectorLabels" . | nindent 4 }}
